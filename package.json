{
  "name": "billetera",
  "version": "1.0.0",
  "description": "Billetera Virtual: app de tarjetas de presentación con login, OCR (OpenAI) y panel admin, sobre Cloudflare Workers + MongoDB Atlas",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "mongodb": "^6.9.0"
  },
  "devDependencies": {
    "wrangler": "^4.103.0"
  }
}
